# Copy to .env (cp .env.example .env) before running the stack.
# These are local, non-sensitive defaults. docker-compose.yml also falls back
# to exactly these values if no .env is present, so the stack starts out of the
# box; override them for any real deployment.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=burgerhuset
