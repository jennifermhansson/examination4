events {}

http {
  server {
    listen 80;
    server_name exam4.jenniferh.dev;

    location /.well-known/acme-challenge/ {
      root /var/www/certbot;
    }

    location / {
      return 200 'waiting for cert';
    }
  }
}
