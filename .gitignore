# Dependencies
node_modules/

# Build output
dist/
build/

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Local-only Docker Compose override (forces nginx to HTTP-only for local runs).
# Must not reach the production server, where the TLS nginx.conf is used.
docker-compose.override.yml
